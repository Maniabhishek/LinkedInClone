import styled from "styled-components";

const Home = (props) => {
    return (<Container>

            <Section>
                <h5>
                    
                </h5>
            </Section>

    </Container>
    );
}

export default Home;